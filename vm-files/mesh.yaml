defaultConfig:
  discoveryAddress: istiod.istio-system.svc:15012
  meshId: mesh1
  proxyMetadata:
    CANONICAL_REVISION: latest
    CANONICAL_SERVICE: wl-legacy-1
    ISTIO_META_CLUSTER_ID: Kubernetes
    ISTIO_META_DNS_AUTO_ALLOCATE: "true"
    ISTIO_META_DNS_CAPTURE: "true"
    ISTIO_META_MESH_ID: mesh1
    ISTIO_META_NETWORK: ""
    ISTIO_META_WORKLOAD_NAME: wl-legacy-1
    ISTIO_METAJSON_LABELS: '{"app":"wl-legacy-1","service.istio.io/canonical-name":"wl-legacy-1","service.istio.io/canonical-version":"latest"}'
    POD_NAMESPACE: vm
    SERVICE_ACCOUNT: vm-access
    TRUST_DOMAIN: cluster.local
  tracing:
    zipkin:
      address: zipkin.istio-system:9411
